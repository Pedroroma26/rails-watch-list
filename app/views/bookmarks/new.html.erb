<h1>New review</h1>
<%= simple_form_for [@list, @bookmark] do |f| %>
  <%= f.input :comment %>
  <%= f.association :movie %>
  <%= f.submit "Submit", class: "btn btn-primary" %>
<% end %>

<p></p>

<div>
  <%= link_to "Go Back", list_path(@list), class: "btn btn-primary" %>
</div>
