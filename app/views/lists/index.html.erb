<h2>Lists</h2>
<div>
<% @lists.each do |list| %>
  <div class="container">
  <p class="name"><strong><%= link_to list.name, list_path(list) %></strong></p>
  </div>
<% end %>
<div>

<div>
  <%= link_to "Create List", new_list_path, class: "btn btn-primary" %>
</div>
